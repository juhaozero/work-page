{
  "name": "work-page",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "predev": "npm run generate:projects",
    "build": "npm run prepare:content && astro build",
    "prepare:content": "npm run validate:projects && npm run generate:projects && npm run probe:health && npm run generate:og",
    "preview": "astro preview",
    "astro": "astro",
    "generate:projects": "node scripts/generate-projects.mjs",
    "validate:projects": "node scripts/validate-projects.mjs",
    "probe:health": "node scripts/probe-health.mjs",
    "generate:og": "node scripts/generate-og.mjs",
    "generate:favicon": "node scripts/generate-favicon.mjs"
  },
  "dependencies": {
    "@astrojs/react": "^6.0.1",
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "astro": "^7.0.6",
    "framer-motion": "^12.42.2",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "to-ico": "^1.1.5"
  }
}
